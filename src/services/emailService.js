const wait = () => new Promise((resolve) => window.setTimeout(resolve, 700 + Math.floor(Math.random() * 701)));
const details = (interview) => `${interview.interviewType} interview for ${interview.jobTitle} on ${interview.date} at ${interview.time}`;
export const generateInterviewInvitation = (interview, candidate, company = 'HireFlow AI', recruiter = 'Ankit Kumar') => ({ subject: `Interview invitation: ${interview.jobTitle} at ${company}`, body: `Dear ${candidate.name},\n\nWe are pleased to invite you to a ${details(interview)}. The session will be ${interview.duration} minutes with ${interview.interviewer}.\n\n${interview.meetingLink ? `Join: ${interview.meetingLink}` : `Location: ${interview.location || 'To be confirmed'}`}\n\nRegards,\n${recruiter}\n${company}` });
export const generateInterviewReminder = (interview, candidate) => ({ subject: `Reminder: ${interview.jobTitle} interview`, body: `Dear ${candidate.name}, this is a reminder for your ${details(interview)}.` });
export const generateInterviewRescheduleEmail = (interview, candidate) => ({ subject: `Rescheduled: ${interview.jobTitle} interview`, body: `Dear ${candidate.name}, your interview has been rescheduled to ${interview.date} at ${interview.time}.` });
export const generateInterviewCancellationEmail = (interview, candidate, reason) => ({ subject: `Cancelled: ${interview.jobTitle} interview`, body: `Dear ${candidate.name}, your interview scheduled for ${interview.date} at ${interview.time} has been cancelled. Reason: ${reason}.` });
export const sendMockEmail = async ({ to, subject, body }) => { await wait(); return { id: `MAIL-${Date.now().toString(36).toUpperCase()}`, to, subject, body, status: 'sent', sentAt: new Date().toISOString() }; };
