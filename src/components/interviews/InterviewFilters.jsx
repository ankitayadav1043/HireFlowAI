import { Search, X } from 'lucide-react';
const InterviewFilters = ({ filters, options, onChange, onClear }) => { const field = 'h-10 min-w-0 rounded-xl border border-white/10 bg-slate-950/60 px-3 text-sm text-slate-300 outline-none'; return <section className="rounded-2xl border border-white/10 bg-slate-900/60 p-4"><div className="flex flex-col gap-3 lg:flex-row"><label className="flex h-10 min-w-0 flex-1 items-center gap-2 rounded-xl border border-white/10 bg-slate-950/60 px-3"><Search size={16} className="text-slate-500" /><span className="sr-only">Search interviews</span><input value={filters.search} onChange={(event) => onChange('search', event.target.value)} placeholder="Search candidate, job, interviewer, type or link" className="min-w-0 flex-1 bg-transparent text-sm text-white outline-none" /></label><select value={filters.sort} onChange={(event) => onChange('sort', event.target.value)} className={field} aria-label="Sort interviews"><option value="upcoming">Upcoming first</option><option value="latest">Latest first</option><option value="oldest">Oldest first</option><option value="candidate">Candidate A–Z</option><option value="rating">Highest rating</option><option value="status">Status</option></select></div><div className="mt-3 grid gap-3 sm:grid-cols-2 lg:grid-cols-4 xl:grid-cols-7"><select value={filters.status} onChange={(event) => onChange('status', event.target.value)} className={field}><option value="">All statuses</option>{options.statuses.map((item) => <option key={item}>{item}</option>)}</select><select value={filters.type} onChange={(event) => onChange('type', event.target.value)} className={field}><option value="">All types</option>{options.types.map((item) => <option key={item}>{item}</option>)}</select><select value={filters.job} onChange={(event) => onChange('job', event.target.value)} className={field}><option value="">All jobs</option>{options.jobs.map((item) => <option key={item.id} value={item.id}>{item.title}</option>)}</select><select value={filters.interviewer} onChange={(event) => onChange('interviewer', event.target.value)} className={field}><option value="">All interviewers</option>{options.interviewers.map((item) => <option key={item}>{item}</option>)}</select><input aria-label="Start date" type="date" value={filters.from} onChange={(event) => onChange('from', event.target.value)} className={field} /><input aria-label="End date" type="date" value={filters.to} onChange={(event) => onChange('to', event.target.value)} className={field} /><select value={filters.rating} onChange={(event) => onChange('rating', event.target.value)} className={field}><option value="">Any rating</option>{[5,4,3,2,1].map((item) => <option key={item} value={item}>{item}+ stars</option>)}</select><select value={filters.reminder} onChange={(event) => onChange('reminder', event.target.value)} className={field}><option value="">Any reminder</option><option value="sent">Sent</option><option value="pending">Pending</option></select></div><button type="button" onClick={onClear} className="mt-3 inline-flex items-center gap-1 text-xs text-slate-500"><X size={13} />Clear filters</button></section>; };
export default InterviewFilters;
