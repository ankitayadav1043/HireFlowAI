import { CalendarDays, ExternalLink, X } from 'lucide-react';
import { formatInterviewDate, formatInterviewTime } from '../../utils/dateTime';
import InterviewStatusBadge from './InterviewStatusBadge';
const InterviewDetailsModal = ({ interview, candidate, activities, onClose, onEdit, onFeedback, onQuestions, onInvitation }) => { if(!interview)return null; const related=activities.filter(item=>item.relatedId===interview.id).sort((a,b)=>(b.timestamp||'').localeCompare(a.timestamp||'')); return <div className="fixed inset-0 z-[75] overflow-y-auto bg-slate-950/85 p-4" role="dialog" aria-modal="true"><div className="mx-auto my-4 max-w-3xl rounded-3xl border border-white/10 bg-slate-900 p-6"><div className="flex items-start justify-between"><div><p className="text-xs text-cyan-300">Interview details</p><h2 className="text-xl font-semibold text-white">{interview.candidateName} · {interview.jobTitle}</h2><div className="mt-2"><InterviewStatusBadge status={interview.status}/></div></div><button type="button" onClick={onClose}><X className="text-slate-500"/></button></div><div className="mt-6 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{[['Match score',`${candidate?.matchScore||0}%`],['Type',interview.interviewType],['Interviewer',interview.interviewer],['Date',formatInterviewDate(interview.date)],['Time',formatInterviewTime(interview.time)],['Duration',`${interview.duration||0} minutes`],['Mode',interview.meetingMode||'Online'],['Reminder',interview.reminderSent?'Sent':'Pending'],['Rating',interview.rating??'Not rated']].map(([label,value])=><div key={label} className="rounded-xl bg-white/[0.03] p-3"><p className="text-xs text-slate-500">{label}</p><p className="mt-1 text-sm text-slate-200">{value||'Not available'}</p></div>)}</div>{(interview.meetingLink||interview.location)&&<a href={interview.meetingLink||'#'} target="_blank" rel="noreferrer" className="mt-4 inline-flex items-center gap-2 text-sm text-cyan-300"><ExternalLink size={14}/>{interview.meetingLink||interview.location}</a>}<div className="mt-5 grid gap-4 sm:grid-cols-2"><div><p className="text-xs text-slate-500">Notes</p><p className="mt-1 text-sm text-slate-300">{interview.notes||'No notes'}</p></div><div><p className="text-xs text-slate-500">Recommendation</p><p className="mt-1 text-sm text-slate-300">{interview.recommendation||'Not provided'}</p></div></div>{interview.generatedQuestions?.length>0&&<div className="mt-5"><h3 className="text-sm font-semibold text-white">Generated questions</h3><ol className="mt-2 list-decimal space-y-1 pl-5 text-sm text-slate-400">{interview.generatedQuestions.map((question,index)=><li key={`${question}-${index}`}>{question}</li>)}</ol></div>}{interview.feedback&&<div className="mt-5 rounded-xl bg-emerald-500/5 p-4"><p className="text-xs text-emerald-300">Feedback</p><p className="mt-1 text-sm text-slate-300">{interview.feedback}</p></div>}<div className="mt-5"><h3 className="text-sm font-semibold text-white">Related activity</h3>{related.length?<ol className="mt-2 space-y-2">{related.map(item=><li key={item.id} className="flex gap-2 text-xs text-slate-500"><CalendarDays size={13}/><span>{item.title} · {item.timestamp?new Date(item.timestamp).toLocaleString('en-IN'):'Time unavailable'}</span></li>)}</ol>:<p className="mt-2 text-xs text-slate-600">No related activity</p>}</div><div className="mt-6 flex flex-wrap justify-end gap-2"><button type="button" onClick={onQuestions} className="rounded-xl border border-white/10 px-3 py-2 text-sm">Questions</button><button type="button" onClick={onInvitation} className="rounded-xl border border-white/10 px-3 py-2 text-sm">Invitation</button><button type="button" onClick={onFeedback} className="rounded-xl border border-white/10 px-3 py-2 text-sm">Feedback</button><button type="button" onClick={onEdit} className="rounded-xl bg-cyan-500 px-4 py-2 text-sm font-semibold text-slate-950">Edit / Reschedule</button></div></div></div>; };
export default InterviewDetailsModal;
