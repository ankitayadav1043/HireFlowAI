import { useState } from 'react';
import { AlertTriangle, X } from 'lucide-react';
import { formatInterviewDate, formatInterviewTime } from '../../utils/dateTime';
const CancelInterviewModal = ({ interview, onClose, onConfirm }) => { const [reason, setReason] = useState(''); const [confirmed, setConfirmed] = useState(false); if (!interview) return null; return <div className="fixed inset-0 z-[80] grid place-items-center bg-slate-950/85 p-4" role="dialog" aria-modal="true"><div className="w-full max-w-lg rounded-3xl border border-white/10 bg-slate-900 p-6"><div className="flex gap-3"><AlertTriangle className="text-rose-300" /><div className="flex-1"><h2 className="text-xl font-semibold text-white">Cancel interview?</h2><p className="mt-1 text-sm text-slate-400">{interview.candidateName} · {interview.jobTitle}</p><p className="text-xs text-slate-500">{formatInterviewDate(interview.date)} at {formatInterviewTime(interview.time)}</p></div><button type="button" onClick={onClose}><X className="text-slate-500" /></button></div><label className="mt-5 block text-xs text-slate-400">Cancellation reason<textarea value={reason} onChange={(event) => setReason(event.target.value)} rows="3" className="mt-2 w-full rounded-xl border border-white/10 bg-slate-950 p-3 text-sm text-white" /></label><label className="mt-3 flex gap-2 text-sm text-slate-400"><input type="checkbox" checked={confirmed} onChange={(event) => setConfirmed(event.target.checked)} />I confirm this interview should be cancelled.</label><div className="mt-5 flex justify-end gap-3"><button type="button" onClick={onClose} className="rounded-xl border border-white/10 px-4 py-2 text-sm">Keep interview</button><button type="button" disabled={!reason.trim() || !confirmed} onClick={() => onConfirm(reason.trim())} className="rounded-xl bg-rose-500 px-4 py-2 text-sm font-semibold text-white disabled:opacity-40">Cancel interview</button></div></div></div>; };
export default CancelInterviewModal;
