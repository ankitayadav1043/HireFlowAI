import { CalendarDays, Clock, Copy, Eye, MapPin, Pencil, Star, Video } from 'lucide-react';
import { formatInterviewDate, formatInterviewTime } from '../../utils/dateTime';
import InterviewStatusBadge from './InterviewStatusBadge';
const InterviewCard = ({ interview, onView, onEdit, onCancel, onFeedback, onQuestions, onInvitation, onCopyLink }) => <article className="flex min-w-0 flex-col rounded-2xl border border-white/10 bg-slate-900/70 p-5"><div className="flex items-start justify-between gap-3"><div className="min-w-0"><h2 className="truncate font-semibold text-white">{interview.candidateName || 'Unknown candidate'}</h2><p className="truncate text-sm text-cyan-300">{interview.jobTitle || 'Unassigned job'}</p></div><InterviewStatusBadge status={interview.status} /></div><div className="mt-4 grid grid-cols-2 gap-3 text-xs text-slate-400"><span>{interview.interviewType || 'Interview'}</span><span>{interview.interviewer || 'No interviewer'}</span><span className="inline-flex gap-1"><CalendarDays size={13} />{formatInterviewDate(interview.date)}</span><span className="inline-flex gap-1"><Clock size={13} />{formatInterviewTime(interview.time)} · {interview.duration || 0}m</span></div><div className="mt-4 flex items-center justify-between rounded-xl bg-white/[0.03] p-3 text-xs"><span className="inline-flex items-center gap-1 text-slate-400">{interview.meetingMode === 'In-person' ? <MapPin size={13} /> : <Video size={13} />}{interview.meetingMode || (interview.meetingLink ? 'Online' : 'Not specified')}</span><span className="inline-flex items-center gap-1 text-amber-300"><Star size={13} />{interview.rating ?? '—'}</span></div><div className="mt-auto flex flex-wrap gap-1 border-t border-white/10 pt-4"><button type="button" onClick={onView} className="rounded-lg bg-cyan-500 px-2.5 py-1.5 text-xs font-semibold text-slate-950"><Eye size={13} /></button><button type="button" onClick={onEdit} className="p-2 text-slate-500" aria-label="Edit interview"><Pencil size={14} /></button>{interview.meetingLink && <button type="button" onClick={onCopyLink} className="p-2 text-slate-500" aria-label="Copy meeting link"><Copy size={14} /></button>}<button type="button" onClick={onQuestions} className="rounded-lg px-2 py-1 text-xs text-violet-300">Questions</button><button type="button" onClick={onInvitation} className="rounded-lg px-2 py-1 text-xs text-blue-300">Invite</button>{interview.status !== 'Completed' && interview.status !== 'Cancelled' && <button type="button" onClick={onFeedback} className="rounded-lg px-2 py-1 text-xs text-emerald-300">Complete</button>}{interview.status !== 'Cancelled' && <button type="button" onClick={onCancel} className="ml-auto rounded-lg px-2 py-1 text-xs text-rose-300">Cancel</button>}</div></article>;
export default InterviewCard;
