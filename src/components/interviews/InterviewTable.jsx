import { Copy, Eye, MessageSquare, Pencil, Trash2 } from 'lucide-react';
import { formatInterviewDate, formatInterviewTime } from '../../utils/dateTime';
import InterviewStatusBadge from './InterviewStatusBadge';
const InterviewTable = ({ interviews, onView, onEdit, onCancel, onFeedback, onCopyLink }) => <div className="max-w-full overflow-x-auto rounded-2xl border border-white/10 bg-slate-900/70"><table className="w-full min-w-[1200px] text-left text-sm"><thead className="border-b border-white/10 bg-slate-950/40 text-xs uppercase text-slate-500"><tr>{['Candidate','Job','Type','Interviewer','Date and time','Duration','Status','Rating','Reminder','Actions'].map((item) => <th key={item} className="px-4 py-3 font-medium">{item}</th>)}</tr></thead><tbody className="divide-y divide-white/[0.07]">{interviews.map((interview) => <tr key={interview.id} className="hover:bg-white/[0.025]"><td className="px-4 py-4"><button type="button" onClick={() => onView(interview)} className="font-medium text-white hover:text-cyan-300">{interview.candidateName || 'Unknown'}</button></td><td className="px-4 py-4 text-slate-400">{interview.jobTitle || '—'}</td><td className="px-4 py-4 text-slate-400">{interview.interviewType || '—'}</td><td className="px-4 py-4 text-slate-400">{interview.interviewer || '—'}</td><td className="px-4 py-4 text-slate-400">{formatInterviewDate(interview.date)}<p className="text-xs">{formatInterviewTime(interview.time)}</p></td><td className="px-4 py-4 text-slate-400">{interview.duration || 0}m</td><td className="px-4 py-4"><InterviewStatusBadge status={interview.status} /></td><td className="px-4 py-4 text-amber-300">{interview.rating ?? '—'}</td><td className="px-4 py-4 text-slate-400">{interview.reminderSent ? 'Sent' : 'Pending'}</td><td className="px-4 py-4"><div className="flex gap-1"><button type="button" onClick={() => onView(interview)} aria-label="View interview" className="p-2 text-slate-500"><Eye size={14} /></button><button type="button" onClick={() => onEdit(interview)} aria-label="Edit interview" className="p-2 text-slate-500"><Pencil size={14} /></button>{interview.meetingLink && <button type="button" onClick={() => onCopyLink(interview)} aria-label="Copy link" className="p-2 text-slate-500"><Copy size={14} /></button>}<button type="button" onClick={() => onFeedback(interview)} aria-label="Add feedback" className="p-2 text-emerald-400"><MessageSquare size={14} /></button><button type="button" onClick={() => onCancel(interview)} aria-label="Cancel interview" className="p-2 text-rose-400"><Trash2 size={14} /></button></div></td></tr>)}</tbody></table></div>;
export default InterviewTable;
