import { X } from 'lucide-react'; import AutomationStatusBadge from './AutomationStatusBadge'; import AutomationTriggerBadge from './AutomationTriggerBadge';
export default function AutomationDetailsModal({ item, onClose, onRun }) { if(!item)return null; return <div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/80 p-4" role="dialog" aria-modal="true"><div className="max-h-[90vh] w-full max-w-2xl overflow-y-auto rounded-3xl border border-white/10 bg-slate-900 p-5"><div className="flex justify-between"><div><p className="text-xs text-cyan-300">{item.category}</p><h2 className="text-xl font-semibold text-white">{item.name}</h2></div><button onClick={onClose} aria-label="Close"><X/></button></div><div className="mt-3 flex gap-2"><AutomationStatusBadge status={item.status}/><AutomationTriggerBadge trigger={item.triggerType}/></div><p className="mt-4 text-slate-300">{item.description}</p><h3 className="mt-5 font-medium text-white">Execution steps</h3><ol className="mt-3 space-y-2">{item.steps?.map(step=><li key={step.order} className="rounded-xl bg-slate-950 p-3 text-sm"><span className="mr-2 text-cyan-300">{step.order}.</span>{step.label}<span className="ml-2 text-slate-500">{step.actionType}</span></li>)}</ol><button onClick={()=>onRun(item)} className="mt-5 rounded-xl bg-cyan-500 px-4 py-2 font-semibold text-slate-950">Run now</button></div></div>; }
