import { Bot, CheckCircle2, Clock3, Gauge, Play, TriangleAlert, Zap } from 'lucide-react';
const icons = [Bot, Zap, Play, CheckCircle2, TriangleAlert, Gauge, Clock3];
export default function AutomationMetrics({ metrics }) { const values = [['Total Automations', metrics.total], ['Active Automations', metrics.active], ['Runs Today', metrics.runsToday], ['Successful Runs', metrics.successfulRuns], ['Failed Runs', metrics.failedRuns], ['Success Rate', `${metrics.successRate}%`], ['Hours Saved', metrics.estimatedHoursSaved]]; return <section className="grid gap-3 sm:grid-cols-2 xl:grid-cols-7" aria-label="Automation metrics">{values.map(([label,value],i)=>{const Icon=icons[i];return <div key={label} className="rounded-2xl border border-white/10 bg-slate-900/70 p-4"><Icon size={18} className="mb-3 text-cyan-300"/><p className="text-2xl font-semibold text-white">{value}</p><p className="text-xs text-slate-400">{label}</p></div>;})}</section>; }
